# PROMPT · تحویل به توسعه‌دهنده

> متن زیر را عیناً به همکار (یا به یک ایجنت کدنویسی) بدهید.

---

## نقش و هدف

تو مسئول نگهداری و توسعهٔ یک وب‌سایت **Portfolio + Project Case Studies** هستی.

خروجی صرفاً یک پورتفولیوی معمولی با کارت پروژه نیست؛ هر پروژه یک **صفحهٔ کامل و قابل
ارائه** با Route مستقل دارد، در دو زبان: انگلیسی روی `/` و فارسی روی `/fa`.

سایت یک **بیلد کاملاً استاتیک** است که روی **GitHub Pages** میزبانی می‌شود. هیچ سروری،
هیچ دیتابیسی و هیچ پنل ادمینی وجود ندارد. محتوای پروژه‌ها فایل‌های Markdown داخل خود ریپو
هستند و هر پوش روی `main` سایت را از نو می‌سازد و منتشر می‌کند.

این سایت قبلاً یک اپلیکیشن Astro SSR روی VPS با SQLite و پنل ادمین بود. آن معماری آگاهانه
کنار گذاشته شد. **برنگردان.**

## اسناد

دو فایل در ریشهٔ پروژه هست. **قبل از نوشتن حتی یک خط کد، هر دو را کامل بخوان:**

- ‏**`PLAN.md`**: منبع حقیقت معماری. شمای محتوا، مسیرها، قواعد تصویر، استقرار، و
  چک‌لیست ۱۵ موردی verification در آن است.
- ‏**`TASKS.md`**: وضعیت فعلی، روال تکرارشوندهٔ افزودن پروژه، و کارهای باز.

در صورت هر تعارضی، **`PLAN.md` ارجح است**. اگر جایی از `PLAN.md` مبهم بود، **بپرس**؛ حدس
نزن و خودت تصمیم معماری نگیر.

## Stack

```
Astro (static) · React · TypeScript · Tailwind CSS v4
Astro Content Collections + Zod
GitHub Actions · GitHub Pages
```

پایهٔ ظاهری از ریپوی [`Gothsec/Astro-portfolio`](https://github.com/Gothsec/Astro-portfolio)
گرفته شده است.

## ممنوعیت‌ها

اینها را **اضافه نکن**، حتی اگر به نظرت بهتر می‌آیند:

```
هر دیتابیسی · Prisma · هر بک‌اند یا سرویس API
پنل ادمین · احراز هویت · آپلود فایل
CMS · Headless CMS · MDX · Rich Text Editor
آداپتور SSR · میدل‌ور
هر Abstraction، لایه، یا وابستگی‌ای که PLAN.md نخواسته
```

معیار ساده: اگر قابلیتی برای کار کردن به یک پروسهٔ در حال اجرا نیاز دارد، در این معماری جا
ندارد. گیت‌هاب پیج فایل سرو می‌کند و هیچ چیزی اجرا نمی‌کند.

## قواعد غیرقابل‌مذاکره

۱. ‏**همه چیز در زمان بیلد حل می‌شود.** صفحه‌های `[slug]` از طریق `getStaticPaths()` ساخته
   می‌شوند. هیچ داده‌ای در زمان درخواست خوانده نمی‌شود.

۲. ‏**یک منبع حقیقت برای انتشار و ترتیب.** تابع `getPublishedProjects()` در
   `src/lib/projects.ts`. سه جا همین را می‌پرسند؛ اگر هرکدام جداگانه فیلتر کنند، ممکن است
   کارتی در شبکه بیاید که صفحه‌ای پشتش ساخته نشده. فیلتر `published` را جای دیگری تکرار نکن.

۳. ‏**نام فایل همان slug و همان آدرس عمومی است.** تغییر نام یک فایل محتوا یعنی شکستن یک
   لینک. این کار را بدون درخواست صریح انجام نده.

۴. ‏**قاعدهٔ fallback زبان را نشکن.** فیلد فارسی خالی باید به انگلیسی برگردد، نه به جای
   خالی. منطق در `resolveProject()` داخل `src/lib/i18n.ts` است و یک‌جا زندگی می‌کند.

۵. ‏**فیلد `techStack` همتای فارسی ندارد.** نام محصول‌اند و در هر دو زبان لاتین می‌مانند. در
   صفحهٔ فارسی هم باید `dir="ltr"` بگیرند وگرنه برعکس خوانده می‌شوند.

۶. ‏**هرگز HTML دلخواه رندر نکن.** بدون `set:html` روی محتوای فایل‌ها. line breakها را امن
   حفظ کن.

۷. ‏**همهٔ بخش‌های صفحهٔ Case Study اختیاری‌اند** و فقط در صورت غیرخالی بودن رندر می‌شوند.

۸. ‏**فایل `public/.nojekyll` را حذف نکن.** گیت‌هاب پیج Jekyll اجرا می‌کند و Jekyll پوشهٔ
   `_astro/` را دور می‌ریزد، یعنی تمام CSS و جاوااسکریپت سایت. بدون این فایل سایت لخت
   منتشر می‌شود و هیچ خطایی هم نمی‌دهد.

۹. ‏**ریست CSS باید داخل `@layer base` بماند.** در Tailwind v4، CSSِ بدون لایه بر
   utilityهای لایه‌دار غلبه می‌کند و کل سیستم spacing پروژه را از کار می‌اندازد.

۱۰. ‏**بدون SVG در تصاویر پروژه.** یک SVG می‌تواند جاوااسکریپت داخلش داشته باشد و هیچ
    اعتبارسنجی سمت سروری باقی نمانده که جلویش را بگیرد.

۱۱. ‏**تصاویر را سبک نگه دار.** زیر ۵۰۰ کیلوبایت، ترجیحاً `webp`. اینها در تاریخچهٔ گیت
    می‌مانند و پاک نمی‌شوند.

۱۲. ‏**دسترسی‌پذیری پسرفت نکند.** یک `h1` در هر صفحه، سلسله‌مراتب عنوان‌ها بدون پرش، یک
    `main` واحد، skip link سالم، و رعایت `prefers-reduced-motion` در هر انیمیشن جدید.

## روش کار

- ‏قبل از هر تغییری `PLAN.md` را بخوان. اگر تغییر با آن سند نمی‌خواند، **متوقف شو و بپرس**.
- ‏کد را در همان سبک پروژه بنویس: نام‌گذاری، ساختار پوشه، و تراکم کامنت. کامنت‌های این
  پروژه **دلیل** را توضیح می‌دهند نه **کار** را؛ همین کار را بکن.
- ‏فقط چیزی را دست بزن که تغییر لازمش دارد. کد مجاور را «بهتر» نکن.
- ‏هر تغییری را با `npm run build` و بعد `npm run preview` تأیید کن، نه با dev server.
  بخش‌هایی مثل حذف Jekyll و مسیر تصاویر فقط در بیلد production خودشان را نشان می‌دهند.
- ‏در پایان گزارش بده: چه ساخته شد، چه تصمیمی گرفتی، چه چیزی باز ماند.

## شرط اتمام کار

کار وقتی تمام است که:

- ‏هر ۱۵ مورد چک‌لیست verification در بخش ۱۱ فایل `PLAN.md` **سبز** باشد، **و**
- ‏`npm run build` بدون خطا و **بدون هشدار** تمام شود، **و**
- ‏یک پروژهٔ جدید صرفاً با افزودن یک فایل Markdown و چند تصویر، در هر دو زبان روی
  `/projects/[slug]` و `/fa/projects/[slug]` دیده شود.

## اصل راهنما

> **Keep the implementation minimal and maintainable. Do not over-engineer.**
> کوچک‌ترین معماری قابل اتکایی که این نیازمندی‌ها را برآورده کند.
