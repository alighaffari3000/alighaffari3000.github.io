# TASKS · Portfolio + Project Case Studies

> منبع حقیقت معماری: [PLAN.md](PLAN.md) · پرامپت تحویل: [PROMPT.md](PROMPT.md)
> در صورت هرگونه تعارض بین این فایل و `PLAN.md`، **`PLAN.md` ارجح است**.

این فایل قبلاً چهارده تسک ساخت نسخهٔ SSR بود. آن ساخت تمام شد و بعد کنار گذاشته شد، پس
نگه داشتنش به‌عنوان «ترتیب اجرا» بی‌معنی بود. حالا سه چیز را نگه می‌دارد: کاری که انجام
شده، کاری که هر بار تکرار می‌شود، و کاری که باز مانده.

---

## بخش ۱: وضعیت فعلی

| مرحله | وضعیت |
|---|---|
| پورت و سفارشی‌سازی کامپوننت‌های پروژهٔ پایه | انجام شد |
| دوزبانه‌سازی کامل، انگلیسی روی `/` و فارسی روی `/fa` | انجام شد |
| صفحات Case Study با رندر شرطی همهٔ بخش‌ها | انجام شد |
| رفع یافته‌های دسترسی‌پذیری سطح P0 تا P2 | انجام شد |
| مهاجرت از SSR به بیلد استاتیک | انجام شد، کامیت `70e1886` |
| انتقال محتوا از SQLite به Content Collections | انجام شد |
| ورک‌فلو انتشار خودکار روی GitHub Pages | انجام شد |
| تنظیم Pages روی حالت GitHub Actions در ریپو | انجام شد |

مرحله‌هایی که در نسخهٔ سروری ساخته شدند و حالا **عمداً وجود ندارند**: پیکربندی Astro SSR،
شمای Prisma و migrationها، singleton کلاینت Prisma، لایهٔ auth، میدل‌ور گارد، لایهٔ upload،
دو مسیر API احراز هویت، سه مسیر API پروژه و گالری، چهار صفحهٔ پنل ادمین، و استقرار روی VPS
با PM2 و Nginx و Certbot. دلیلش در بخش «به‌روزرسانی» فایل `PLAN.md` است.

---

## بخش ۲: افزودن یک پروژه

این کار تکرارشونده است و هر بار همین پنج قدم را دارد.

### گام ۱ · فایل محتوا

از روی `src/content/projects/example-project.md` یک کپی بگیرید. **نام فایل همان آدرس صفحه
است:** فایل `order-bot.md` روی `/projects/order-bot` و `/fa/projects/order-bot` می‌آید.

نام فایل را با حروف کوچک انگلیسی و خط تیره بنویسید. بعد از انتشار عوضش نکنید، چون آدرس
صفحه را می‌شکند.

### گام ۲ · پر کردن فیلدها

فقط `title` اجباری است. هر فیلد قابل ترجمه یک همتای `*Fa` دارد و اگر فارسی خالی بماند،
انگلیسی در صفحهٔ فارسی رندر می‌شود. فهرست کامل فیلدها با توضیح هرکدام در
`src/content.config.ts` است.

**DoD:** پروژه‌ای که فقط عنوان و توضیح دارد، هیچ سکشن خالی‌ای نمایش نمی‌دهد.

### گام ۳ · تصاویر

فایل‌ها زیر `public/projects/<slug>/` و ارجاع از ریشهٔ سایت:

```
heroImage: /projects/order-bot/hero.webp
```

فرمت `webp` ترجیح دارد، `jpg` و `png` هم قبول‌اند، **`svg` هرگز**. هر فایل زیر ۵۰۰
کیلوبایت بماند، چون اینها در تاریخچهٔ گیت می‌مانند و پاک نمی‌شوند.

**DoD:** تصویر در بیلد production لود می‌شود، نه فقط در dev server.

### گام ۴ · انتشار

مقدار `published: true` و یک `order` بگذارید. عدد کوچک‌تر بالاتر می‌آید. اگر دو پروژه
`order` یکسان داشتند، `date` جدیدتر بالاتر می‌رود.

### گام ۵ · بیلد و پوش

```
npm run build
npm run preview
```

چک‌لیست بخش ۱۱ فایل `PLAN.md` را روی همان بیلد اجرا کنید، بعد به `main` پوش کنید.
ورک‌فلو بقیه‌اش را انجام می‌دهد.

**DoD:** هر دو صفحهٔ انگلیسی و فارسی ساخته می‌شوند، کارت پروژه در شبکه می‌آید، و کنسول
مرورگر خطا ندارد.

---

## بخش ۳: کارهای باز

### ‏B1 · محتوای واقعی پروژه‌ها

سایت هنوز هیچ پروژهٔ منتشرشده‌ای ندارد. سه پروژهٔ تستی نسخهٔ SQLite مهاجرت داده نشدند چون
دادهٔ ساختگی بودند و روی `example.com` اشاره می‌کردند.

**DoD:** دست‌کم یک Case Study واقعی و دوزبانه روی سایت منتشر باشد.

### ‏B2 · تصمیم دربارهٔ SEO

مسیرهای `robots.txt` و `sitemap.xml` و تگ canonical و hreflang مطلق **عمداً** به تعویق
افتاده‌اند تا دامنهٔ نهایی مشخص شود. تگ‌های hreflang فعلی در `Layout.astro` نسبی هستند.

حالا که دامنهٔ `alighaffari3000.github.io` مشخص شده، این تصمیم قابل بازکردن است. اگر
دامنهٔ اختصاصی در راه است، منطقی است تا بعد از آن صبر کنیم، چون هر آدرس مطلقی که الان
نوشته شود بعداً باید عوض شود.

**DoD:** یا پیاده‌سازی شود، یا با یک دلیل مشخص به تعویق بیفتد.

### ‏B3 · پاک‌سازی باقی‌ماندهٔ نسخهٔ سروری

روی دیسک مانده‌اند ولی از گیت خارج شده‌اند: فایل `.env` با اعتبارنامه‌های ادمین که دیگر به
کار نمی‌آیند، و `prisma/dev.db` با همان سه پروژهٔ تستی. هیچ کدی نمی‌خواندشان.

**DoD:** هر دو حذف شوند یا آگاهانه نگه داشته شوند.

### ‏B4 · دامنهٔ اختصاصی

اختیاری. یک فایل `public/CNAME`، رکوردهای DNS، و به‌روزرسانی `site` در `astro.config.mjs`.
روی `B2` اثر می‌گذارد.

---

## اصل راهنما

> **Keep the implementation minimal and maintainable. Do not over-engineer.**
